# docs/priors — trusted prior evidence

These priors are **trusted — do not re-derive them** (handoff §4.3).

**Status: RECONSTRUCTED.** The original `poc/` evidence and memory notes
were not present at seeding (see IMPLEMENTATION-NOTES.md, 2026-07-10). Each
note below restates only what `specs/implementation-handoff.md` and
`specs/mission-control-spec.md` themselves assert about the prior — no
detail has been invented beyond those texts. If the original POC material
surfaces, drop it in here alongside these notes.
