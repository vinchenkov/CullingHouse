# S5 — SQLite WAL + crash discipline on the named volume

Probe, pass/fail criteria, and fallback ADR: `specs/implementation-handoff.md` Part 2.

Deliverable: `RESULT.md` here with a **machine-rerunnable check**. Spike code is
throwaway; its assertions are later folded into the permanent test suite.
